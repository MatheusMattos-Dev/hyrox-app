import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // O selo de desenvolvimento cobre a barra inferior na largura de celular.
  devIndicators: false,
  /* config options here */
};

export default nextConfig;
